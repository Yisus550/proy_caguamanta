@model IEnumerable<proy_caguamanta.Models.DetalleCompra>
@{
    ViewData["Title"] = "Lista de Detalle de Compra";
    Layout = "/Views/Shared/_Layout.cshtml";
}

<h1> LISTA DE ELEMTOS DE LA TABLA DETALLE DE COMPRA</h1>

<div class="container">
    <div class="row mt-5">
        <div class="col-3">
            <a class="btn btn-primary text-white form-control" asp-action="Crear">Crear Detalle de Compra</a>
        </div>
    </div>
    <div class="row mt-5">
        <div class="col-12">
            @if (Model.Count() > 0)
            {
                <table class="table table-striped border">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>IdCompra</th>
                            <th>IdProducto</th>
                            <th>PrecioUnidad</th>
                            <th>Cantidad</th>
                            <th>Importe</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <th>@item.IdCompra</th>
                                <td>@item.IdProducto</td>
                                <td>@item.PrecioUnidad</td>
                                <td>@item.Cantidad</td>
                                <td>@item.Importe</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>NO HAY NINGUN DETALLE DE COMPRA</p>

            }
        </div>
    </div>
</div>
