@model IEnumerable<proy_caguamanta.Models.Cliente>
@{
    ViewData["Title"] = "Lista de Cliente";
    Layout = "/Views/Shared/_Layout.cshtml";
}

<h1> LISTA DE ELEMTOS DE LA TABLA CLIENTE</h1>

<div class="container">
    <div class="row mt-5">
        <div class="col-3">
            <a class="btn btn-primary text-white form-control" asp-action="Crear">Crear Cliente</a>
        </div>
    </div>
    <div class="row mt-5">
        <div class="col-12">
            @if (Model.Count() > 0)
            {
                <table class="table table-striped border">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Nombre</th>
                            <th>Telefono</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <th>@item.Nombre</th>
                                <td>@item.Telefono</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>NO HAY NINGUN CLIENTE</p>

            }
        </div>
    </div>
</div>
